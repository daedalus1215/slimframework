{% extends 'layouts/app.twig' %}



{% block content %}

    {%  for topic in topics %}
        <div class="topic">
            <h4><a href="{{ path_for('topics.show', {id: topic.id}) }}">{{ topic.title }}</a></h4>
        </div>
    {% endfor %}
{% endblock %}