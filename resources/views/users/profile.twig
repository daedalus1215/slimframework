{% extends 'layouts/app.twig' %}

{% block content %}
    <h1>{{ user.username }}</h1>


    <ul>
        <li>{{ user.username }}</li>
        <li>{{ user.first_name }}</li>
        <li>{{ user.last_name }}</li>
    </ul>
{% endblock %}